# Build dependencies (must be first — crepe needs pkg_resources at build time)
setuptools
wheel

# Core
fastapi[standard]
uvicorn[standard]
gunicorn
sqlalchemy
psycopg2-binary
pyjwt
jinja2
python-multipart
aiofiles
httpx
pydantic-settings
alembic

# ML — ASR (use nyrahealth fork for CrisperWhisper in production)
# NOTE: torch/torchaudio are NOT listed here to avoid overwriting
# the CUDA-enabled versions pre-installed on RunPod GPU pods.
# On RunPod: the image already has torch+CUDA. On local dev: install manually:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# Or for CPU-only dev:
#   pip install torch torchaudio
transformers

# ML — Pronunciation
jiwer
g2p-en
nltk

# ML — Prosody
praat-parselmouth
# crepe — installed separately in start_runpod.sh (needs --no-build-isolation)
praatio
soundfile

# ML — Audio
numpy
scipy
librosa
av
noisereduce

# ML — Nearest-neighbor search (MDD phoneme pool)
faiss-cpu

# TTS
piper-tts

# Task queue
celery[redis]
redis

# YOLO export (dev only, not needed in server container)
# ultralytics
# onnx
# onnxruntime
